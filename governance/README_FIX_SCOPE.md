This branch is a consolidation repair, not a new controller program. It is complete only if the existing coordinator can consume compiled current state, resume the current cursor, receive deterministic required-function dispatch, reject illegal epistemic transitions, detect repeated failure signatures, prefer fresher runtime evidence, operate under standing authority, verify/reconcile mutations, and pass the full repository regression suite. Standalone code that is not used by the execution path does not count as completion.
